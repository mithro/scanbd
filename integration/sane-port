service sane-port
{
        port        = 6566
        socket_type = stream
        wait        = no
        user        = saned
        group       = scanner
        server      = /usr/sbin/scanbm
        server_args = scanbm -c /etc/scanbd/scanbd.conf
        disable     = no
}
